# 简单业务模板

**适用场景**：
- 标准 CRUD + 简单业务规则（如状态校验、数据唯一性）
- 少量自定义接口（如审核、激活、分配）
- 简单业务流程（无需工作流引擎）
- 需要数据权限控制

**文档篇幅**：3-5 页

**代码生成支持**：基础 CRUD 生成 + 手动编写业务逻辑

---

## 1. 需求概述

### 1.1 基本信息

| 项目 | 说明 |
|------|------|
| 模块名称 | {模块名} |
| 模块编码 | `{module}.{business}` |
| 文档版本 | 1.0 |
| 创建日期 | YYYY-MM-DD |
| 负责人 | {姓名} |
| 优先级 | P0/P1/P2 |

### 1.2 需求背景

**业务目标**：
- {描述本模块要解决的业务问题}

**用户痛点**：
- {描述当前存在的问题}

**预期价值**：
- {描述功能完成后的收益}

### 1.3 功能范围

**本模块包含的功能**：
- [ ] 标准 CRUD（增删改查）
- [ ] 自定义业务接口
- [ ] 数据导出/导入
- [ ] 其他：{自定义功能}

**本模块不包含的功能**：
- {明确说明不做的功能}

### 1.4 用户角色

| 角色 | 职责 | 典型操作 |
|------|------|---------|
| {角色 1} | {职责描述} | 查询、新增、修改、删除 |
| {角色 2} | {职责描述} | 仅查询、导出 |

### 1.5 使用场景

**场景 1：{场景名称}**
- **触发条件**：{什么情况下触发}
- **参与角色**：{谁参与}
- **操作流程**：{简述操作步骤}
- **预期结果**：{期望达成的结果}

---

## 2. 表结构 (DDL)

### 2.1 表设计

```sql
CREATE TABLE {table_name} (
  id              BIGINT          NOT NULL      AUTO_INCREMENT COMMENT '编号',
  -- 业务字段
  {field_name}    {field_type}    {constraints} COMMENT '{注释}',
  -- 审计字段
  create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  create_by       VARCHAR(64)     DEFAULT ''    COMMENT '创建人',
  update_by       VARCHAR(64)     DEFAULT ''    COMMENT '更新人',
  del_flag        CHAR(1)         DEFAULT '0'   COMMENT '删除标志',
  PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='{表注释}';
```

---

## 3. 业务规则

### 3.1 校验规则

| 规则编号 | 规则名称 | 适用字段 | 校验逻辑 | 错误提示 |
|---------|---------|---------|---------|---------|
| VAL-001 | {规则名} | {字段} | {逻辑} | {提示} |

### 3.2 业务逻辑

| 规则编号 | 规则名称 | 触发时机 | 处理逻辑 | 说明 |
|---------|---------|---------|---------|------|
| BIZ-001 | {规则名} | {时机} | {逻辑} | {说明} |

---

## 4. 自定义接口

### 4.1 接口清单

| 功能 | 方法 | 路径 | 权限标识 | 说明 |
|------|------|------|---------|------|
| {接口名} | POST | `/{module}/{business}/{id}/{action}` | `{module}:{business}:{action}` | {说明} |

---

## 5. 功能清单

### 5.1 标准 CRUD 功能

| 功能 | 方法 | 路径 | 权限标识 |
|------|------|------|---------|
| 查询列表 | GET | `/{module}/{business}/list` | `{module}:{business}:list` |
| 新增 | POST | `/{module}/{business}` | `{module}:{business}:add` |
| 修改 | PUT | `/{module}/{business}` | `{module}:{business}:edit` |
| 删除 | DELETE | `/{module}/{business}/{ids}` | `{module}:{business}:remove` |

### 5.2 自定义功能

| 功能 | 方法 | 路径 | 权限标识 |
|------|------|------|---------|
| {自定义功能} | POST | `/{module}/{business}/{id}/{action}` | `{module}:{business}:{action}` |

---

## 6. 附录

### 6.1 参考示例

- {待补充示例}

---

**文档结束**
