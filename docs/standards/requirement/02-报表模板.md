# 报表模板

**适用场景**：
- 数据查询统计功能（如销售报表、考勤统计）
- 多维度数据分析（如按部门/时间/类别汇总）
- 导出功能为主，无增删改操作
- 复杂查询条件 + 分组聚合

**文档篇幅**：2-3 页

**代码生成支持**：部分支持（需手动编写统计 SQL）

---

## 1. 需求概述

### 1.1 基本信息

| 项目 | 说明 |
|------|------|
| 模块名称 | {模块名} |
| 模块编码 | `{module}.{business}` |
| 文档版本 | 1.0 |
| 创建日期 | YYYY-MM-DD |
| 负责人 | {姓名} |
| 优先级 | P0/P1/P2 |

### 1.2 需求背景

**业务目标**：
- {描述本模块要解决的业务问题}

**用户痛点**：
- {描述当前存在的问题}

**预期价值**：
- {描述功能完成后的收益}

### 1.3 功能范围

**本模块包含的功能**：
- [ ] 数据查询（多条件筛选）
- [ ] 统计汇总（分组聚合）
- [ ] 数据导出（Excel/PDF）
- [ ] 其他：{自定义功能}

**本模块不包含的功能**：
- {明确说明不做的功能}

### 1.4 用户角色

| 角色 | 职责 | 典型操作 |
|------|------|---------|
| {角色 1} | {职责描述} | 查询、导出 |
| {角色 2} | {职责描述} | 仅查询 |

---

## 2. 数据源设计

### 2.1 数据来源

| 数据源 | 类型 | 说明 |
|--------|------|------|
| {table_name} | 主表 | 主要数据来源 |
| {related_table} | 关联表 | JOIN 关联表 |

### 2.2 统计维度

| 维度名称 | 字段 | 分组依据 |
|---------|------|---------|
| {维度 1} | {字段名} | `GROUP BY` 说明 |

### 2.3 统计指标

| 指标名称 | 计算公式 | SQL 表达 |
|---------|---------|---------|
| {指标 1} | {公式} | {SQL} |

---

## 3. 查询条件设计

### 3.1 查询参数

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| {参数 1} | String/Date | 是/否 | - | 说明 |

### 3.2 查询 SQL

```sql
-- 查询 SQL 示例
SELECT {字段列表}
FROM {主表}
LEFT JOIN {关联表} ON {关联条件}
WHERE {条件}
GROUP BY {分组字段}
ORDER BY {排序字段};
```

---

## 4. 功能清单

### 4.1 功能列表

| 功能 | 方法 | 路径 | 权限标识 | 说明 |
|------|------|------|---------|------|
| 查询报表 | GET | `/{module}/{business}/list` | `{module}:{business}:list` | 分页查询 |
| 导出 Excel | POST | `/{module}/{business}/export` | `{module}:{business}:export` | 导出 Excel |

### 4.2 权限标识符

```
{模块}:{业务名}:{操作}
```

---

## 5. 前端页面设计

### 5.1 页面布局

```
{页面布局 ASCII 图}
```

---

## 6. 附录

### 6.1 参考示例

- {待补充示例}

---

**文档结束**
