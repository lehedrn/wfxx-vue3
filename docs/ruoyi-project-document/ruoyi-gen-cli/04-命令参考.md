# ruoyi-gen-cli 命令参考

## 1. 命令格式

```bash
java -jar ruoyi-gen-cli.jar [选项]
```

## 2. 必填参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `--sql` | DDL SQL 文件路径 | `--sql=create_tables.sql` |

## 3. 可选参数

### 3.1 输出控制

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--output` | `./generated.zip` | 输出 ZIP 文件路径 |

**示例**：

```bash
# 输出到指定文件
java -jar ruoyi-gen-cli.jar --sql=ddl.sql --output=/tmp/code.zip

# 输出到当前目录
java -jar ruoyi-gen-cli.jar --sql=ddl.sql --output=./myproject.zip
```

### 3.2 配置控制

| 参数 | 说明 |
|------|------|
| `--config` | YAML 配置文件路径（优先级最高） |

**示例**：

```bash
# 使用配置文件
java -jar ruoyi-gen-cli.jar --sql=ddl.sql --config=gen-config.yml --output=out.zip
```

### 3.3 全局参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--author` | `ruoyi` | 作者名 |
| `--package` | `com.ruoyi.system` | 生成包路径 |
| `--prefix` | - | 表前缀（自动去除） |
| `--web` | `element-plus` | 前端类型 |

**示例**：

```bash
# 指定作者和包名
java -jar ruoyi-gen-cli.jar \
  --sql=ddl.sql \
  --author=dev \
  --package=com.example.system

# 自动去除表前缀
java -jar ruoyi-gen-cli.jar \
  --sql=ddl.sql \
  --prefix=sys_ \
  --web=element-plus
```

## 4. 组合使用

### 4.1 简单模式（仅命令行参数）

```bash
java -jar ruoyi-gen-cli.jar \
  --sql=create_tables.sql \
  --output=./generated.zip \
  --author=dev \
  --package=com.example.demo \
  --prefix=demo_ \
  --web=element-plus
```

### 4.2 配置文件模式（YAML 精细控制）

```bash
java -jar ruoyi-gen-cli.jar \
  --sql=create_tables.sql \
  --config=gen-config.yml \
  --output=./generated.zip
```

### 4.3 混合模式（配置文件 + 命令行覆盖）

命令行参数会覆盖 YAML 全局配置：

```bash
# YAML 配置为主，命令行参数覆盖 author 和 package
java -jar ruoyi-gen-cli.jar \
  --sql=create_tables.sql \
  --config=gen-config.yml \
  --output=./generated.zip \
  --author=override-author \
  --package=com.override.package
```

## 5. 参数优先级

```
YAML 表级/列级配置（最高）
    ↓
命令行参数
    ↓
YAML 全局配置
    ↓
generator.yml 默认值（最低）
```

## 6. 错误处理

### 6.1 缺少必填参数

```
错误：请指定 --sql 参数，如 --sql=create_tables.sql
```

### 6.2 SQL 文件不存在

```
错误：SQL 文件不存在：/path/to/create_tables.sql
```

### 6.3 未找到 CREATE TABLE 语句

```
警告：未在 SQL 文件中找到 CREATE TABLE 语句
```

### 6.4 配置文件不存在

```
错误：配置文件不存在：/path/to/gen-config.yml
```

## 7. 完整命令示例

### 示例 1：快速生成单表代码

```bash
java -jar ruoyi-gen-cli.jar \
  --sql=student.sql \
  --output=./student.zip \
  --author=dev \
  --package=com.example.demo \
  --prefix=demo_
```

### 示例 2：使用配置文件生成树表

```bash
java -jar ruoyi-gen-cli.jar \
  --sql=category.sql \
  --config=category-config.yml \
  --output=./category.zip
```

### 示例 3：批量生成多表代码

```bash
java -jar ruoyi-gen-cli.jar \
  --sql=all_tables.sql \
  --config=gen-config.yml \
  --output=./all.zip
```

### 示例 4：生成主子表代码

```bash
java -jar ruoyi-gen-cli.jar \
  --sql=customer_goods.sql \
  --config=sub-table-config.yml \
  --output=./customer.zip
```

## 8. 输出日志

成功生成后的日志示例：

```
2026-04-07 10:00:00 INFO  加载配置文件：/path/to/gen-config.yml
2026-04-07 10:00:01 INFO  正在解析 DDL 文件：/path/to/create_tables.sql
2026-04-07 10:00:01 INFO  解析到 3 张表
2026-04-07 10:00:02 INFO  代码生成完成！输出文件：/path/to/output.zip
2026-04-07 10:00:02 INFO  生成表清单：
2026-04-07 10:00:02 INFO    - demo_student (学生信息表) → Student [tpl=crud, web=element-plus, pkg=com.example.demo]
2026-04-07 10:00:02 INFO    - demo_category (类别表) → Category [tpl=tree, web=element-plus, pkg=com.example.demo]
2026-04-07 10:00:02 INFO    - demo_customer (客户表) → Customer [tpl=sub, web=element-plus, pkg=com.example.demo]
```

---

**文档版本**: 1.0  
**最后更新**: 2026-04-07  
**基于版本**: ruoyi-gen-cli 1.0
