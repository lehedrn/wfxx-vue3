# 页面模块

## 概述

若依前端的页面组件位于 `src/views/` 目录下，按照功能模块组织，包含登录、首页、系统管理、系统监控、系统工具等页面。

**目录结构**:

```
src/views/
├── index.vue                    # 首页
├── login.vue                    # 登录页
├── register.vue                 # 注册页
├── lock.vue                     # 锁屏页
├── redirect/                    # 重定向页面
├── error/                       # 错误页面
│   ├── 401.vue                  # 未授权页面
│   └── 404.vue                  # 未找到页面
│
├── system/                      # 系统管理模块
│   ├── user/                    # 用户管理
│   │   ├── index.vue            # 用户列表
│   │   └── authRole.vue         # 分配角色
│   ├── role/                    # 角色管理
│   │   ├── index.vue            # 角色列表
│   │   ├── authUser.vue         # 分配用户
│   │   └── selectUser.vue       # 选择用户
│   ├── menu/                    # 菜单管理
│   ├── dept/                    # 部门管理
│   ├── post/                    # 岗位管理
│   ├── dict/                    # 字典管理
│   │   ├── index.vue            # 字典类型列表
│   │   └── data.vue             # 字典数据列表
│   ├── config/                  # 参数配置
│   └── notice/                  # 通知公告
│
├── monitor/                     # 系统监控模块
│   ├── online/                  # 在线用户
│   ├── job/                     # 定时任务
│   │   ├── index.vue            # 任务列表
│   │   ├── log.vue              # 任务日志
│   │   └── detail.vue           # 任务详情
│   ├── logininfor/              # 登录日志
│   ├── operlog/                 # 操作日志
│   │   └── detail.vue           # 日志详情
│   ├── druid/                   # Druid 监控
│   └── server/                  # 服务器监控
│
├── tool/                        # 系统工具模块
│   ├── build/                   # 表单构建
│   ├── gen/                     # 代码生成
│   │   ├── index.vue            # 生成列表
│   │   ├── editTable.vue        # 编辑表
│   │   ├── genInfoForm.vue      # 生成信息
│   │   └── basicInfoForm.vue    # 基本信息
│   └── swagger/                 # Swagger 文档
│
└── demo/                        # Demo 示例模块
    ├── customer/                # 客户管理
    ├── product/                 # 产品管理
    └── student/                 # 学生管理
```

---

## 标准页面结构

若依的标准列表页面遵循统一的代码结构和开发模式。

### 基础模板

```vue
<template>
  <div class="app-container">
    <!-- 搜索表单 -->
    <el-form :model="queryParams" ref="queryRef" :inline="true" v-show="showSearch" label-width="68px">
      <el-form-item label="用户名" prop="userName">
        <el-input
          v-model="queryParams.userName"
          placeholder="请输入用户名"
          clearable
          @keyup.enter="handleQuery"
        />
      </el-form-item>
      
      <el-form-item label="状态" prop="status">
        <el-select v-model="queryParams.status" placeholder="请选择状态" clearable>
          <el-option
            v-for="dict in sys_normal_disable"
            :key="dict.value"
            :label="dict.label"
            :value="dict.value"
          />
        </el-select>
      </el-form-item>
      
      <el-form-item>
        <el-button type="primary" icon="Search" @click="handleQuery">搜索</el-button>
        <el-button icon="Refresh" @click="resetQuery">重置</el-button>
      </el-form-item>
    </el-form>

    <!-- 操作按钮 -->
    <el-row :gutter="10" class="mb8">
      <el-col :span="1.5">
        <el-button type="primary" plain icon="Plus" @click="handleAdd" v-hasPermi="['system:user:add']">新增</el-button>
      </el-col>
      <el-col :span="1.5">
        <el-button type="danger" plain icon="Delete" :disabled="multiple" @click="handleDelete" v-hasPermi="['system:user:remove']">删除</el-button>
      </el-col>
      <el-col :span="1.5">
        <el-button type="warning" plain icon="Download" @click="handleExport" v-hasPermi="['system:user:export']">导出</el-button>
      </el-col>
      
      <right-toolbar v-model:showSearch="showSearch" @queryTable="getList" :columns="columns"></right-toolbar>
    </el-row>

    <!-- 数据表格 -->
    <el-table v-loading="loading" :data="userList" @selection-change="handleSelectionChange">
      <el-table-column type="selection" width="55" align="center" />
      <el-table-column label="用户 ID" prop="userId" v-if="columns[0].visible" />
      <el-table-column label="用户名" prop="userName" v-if="columns[1].visible" :show-overflow-tooltip="true" />
      <el-table-column label="昵称" prop="nickName" v-if="columns[2].visible" :show-overflow-tooltip="true" />
      <el-table-column label="状态" prop="status" v-if="columns[3].visible">
        <template #default="scope">
          <dict-tag :options="sys_normal_disable" :value="scope.row.status" />
        </template>
      </el-table-column>
      <el-table-column label="创建时间" prop="createTime" v-if="columns[4].visible" width="180">
        <template #default="scope">
          <span>{{ parseTime(scope.row.createTime, '{y}-{m}-{d}') }}</span>
        </template>
      </el-table-column>
      <el-table-column label="操作" align="center" width="180" class-name="small-padding fixed-width">
        <template #default="scope">
          <el-button link type="primary" icon="Edit" @click="handleUpdate(scope.row)" v-hasPermi="['system:user:edit']">编辑</el-button>
          <el-button link type="primary" icon="Delete" @click="handleDelete(scope.row)" v-hasPermi="['system:user:remove']">删除</el-button>
        </template>
      </el-table-column>
    </el-table>
    
    <!-- 分页 -->
    <pagination
      v-show="total>0"
      :total="total"
      v-model:page="queryParams.pageNum"
      v-model:limit="queryParams.pageSize"
      @pagination="getList"
    />

    <!-- 新增/编辑对话框 -->
    <el-dialog :title="title" v-model="open" width="600px" append-to-body>
      <el-form ref="userRef" :model="form" :rules="rules" label-width="80px">
        <el-form-item label="用户名" prop="userName">
          <el-input v-model="form.userName" placeholder="请输入用户名" />
        </el-form-item>
        
        <el-form-item label="昵称" prop="nickName">
          <el-input v-model="form.nickName" placeholder="请输入昵称" />
        </el-form-item>
        
        <el-form-item label="状态" prop="status">
          <el-radio-group v-model="form.status">
            <el-radio
              v-for="dict in sys_normal_disable"
              :key="dict.value"
              :value="dict.value"
            >{{ dict.label }}</el-radio>
          </el-radio-group>
        </el-form-item>
      </el-form>
      
      <template #footer>
        <div class="dialog-footer">
          <el-button type="primary" @click="submitForm">确 定</el-button>
          <el-button @click="cancel">取 消</el-button>
        </div>
      </template>
    </el-dialog>
  </div>
</template>

<script setup name="UserList">
import { listUser, getUser, addUser, updateUser, delUser } from "@/api/system/user"
import { useDict } from '@/utils/dict'

const { proxy } = getCurrentInstance()
const { sys_normal_disable } = useDict('sys_normal_disable')

const userList = ref([])
const open = ref(false)
const loading = ref(true)
const showSearch = ref(true)
const ids = ref([])
const total = ref(0)
const title = ref("")
const queryParams = ref({
  pageNum: 1,
  pageSize: 10,
  userName: undefined,
  status: undefined
})

const columns = ref([
  { key: 0, label: `用户 ID`, prop: `userId`, visible: true },
  { key: 1, label: `用户名`, prop: `userName`, visible: true },
  { key: 2, label: `昵称`, prop: `nickName`, visible: true },
  { key: 3, label: `状态`, prop: `status`, visible: true },
  { key: 4, label: `创建时间`, prop: `createTime`, visible: true }
])

const userFormSchema = {
  userId: [{ required: true, message: "用户 ID 不能为空", trigger: "blur" }],
  userName: [{ required: true, message: "用户名不能为空", trigger: "blur" }],
  nickName: [{ required: true, message: "昵称不能为空", trigger: "blur" }],
  status: [{ required: true, message: "状态不能为空", trigger: "change" }]
}

const form = ref({})
const rules = ref(userFormSchema)

// 查询列表
const getList = () => {
  loading.value = true
  listUser(queryParams.value).then(response => {
    userList.value = response.rows
    total.value = response.total
    loading.value = false
  })
}

// 搜索按钮操作
const handleQuery = () => {
  queryParams.value.pageNum = 1
  getList()
}

// 重置按钮操作
const resetQuery = () => {
  proxy.resetForm("queryRef")
  handleQuery()
}

// 多选框选中数据
const handleSelectionChange = (selection) => {
  ids.value = selection.map(item => item.userId)
}

// 新增按钮操作
const handleAdd = () => {
  reset()
  open.value = true
  title.value = "添加用户"
}

// 修改按钮操作
const handleUpdate = (row) => {
  reset()
  const userId = row.userId || ids.value
  getUser(userId).then(response => {
    form.value = response.data
    open.value = true
    title.value = "修改用户"
  })
}

// 表单提交
const submitForm = () => {
  proxy.$refs["userRef"].validate(valid => {
    if (valid) {
      loading.value = true
      if (form.value.userId != undefined) {
        updateUser(form.value).then(response => {
          proxy.$modal.msgSuccess("修改成功")
          open.value = false
          getList()
        })
      } else {
        addUser(form.value).then(response => {
          proxy.$modal.msgSuccess("新增成功")
          open.value = false
          getList()
        })
      }
    }
  })
}

// 删除按钮操作
const handleDelete = (row) => {
  const userIds = row.userId || ids.value
  proxy.$modal.confirm('是否确认删除用户编号为"' + userIds + '"的数据项？').then(function() {
    return delUser(userIds)
  }).then(() => {
    getList()
    proxy.$modal.msgSuccess("删除成功")
  }).catch(() => {})
}

// 导出按钮操作
const handleExport = () => {
  proxy.download('system/user/export', queryParams.value, 'user_export.xlsx')
}

// 表单重置
const reset = () => {
  form.value = {
    userId: undefined,
    userName: undefined,
    nickName: undefined,
    status: undefined
  }
  proxy.resetForm("userRef")
}

// 取消按钮
const cancel = () => {
  open.value = false
  reset()
}

getList()
</script>
```

---

## 标准页面要素

### 1. 搜索表单

```vue
<el-form :model="queryParams" ref="queryRef" :inline="true" v-show="showSearch" label-width="68px">
  <el-form-item label="用户名" prop="userName">
    <el-input
      v-model="queryParams.userName"
      placeholder="请输入用户名"
      clearable
      @keyup.enter="handleQuery"
    />
  </el-form-item>
  
  <el-form-item>
    <el-button type="primary" icon="Search" @click="handleQuery">搜索</el-button>
    <el-button icon="Refresh" @click="resetQuery">重置</el-button>
  </el-form-item>
</el-form>
```

**要点**:
- `ref="queryRef"` 用于重置表单
- `@keyup.enter="handleQuery"` 支持回车搜索
- `clearable` 快速清空输入

### 2. 操作按钮区

```vue
<el-row :gutter="10" class="mb8">
  <el-col :span="1.5">
    <el-button type="primary" plain icon="Plus" @click="handleAdd" v-hasPermi="['system:user:add']">新增</el-button>
  </el-col>
  <el-col :span="1.5">
    <el-button type="danger" plain icon="Delete" :disabled="multiple" @click="handleDelete" v-hasPermi="['system:user:remove']">删除</el-button>
  </el-col>
  <el-col :span="1.5">
    <el-button type="warning" plain icon="Download" @click="handleExport" v-hasPermi="['system:user:export']">导出</el-button>
  </el-col>
  
  <right-toolbar v-model:showSearch="showSearch" @queryTable="getList" :columns="columns"></right-toolbar>
</el-row>
```

**要点**:
- 使用 `v-hasPermi` 控制按钮权限
- 删除按钮 `:disabled="multiple"` 未选中时禁用
- `right-toolbar` 提供搜索切换和列显隐

### 3. 数据表格

```vue
<el-table v-loading="loading" :data="userList" @selection-change="handleSelectionChange">
  <el-table-column type="selection" width="55" align="center" />
  
  <el-table-column label="用户名" prop="userName" :show-overflow-tooltip="true" />
  
  <el-table-column label="状态" prop="status">
    <template #default="scope">
      <dict-tag :options="sys_normal_disable" :value="scope.row.status" />
    </template>
  </el-table-column>
  
  <el-table-column label="创建时间" prop="createTime" width="180">
    <template #default="scope">
      <span>{{ parseTime(scope.row.createTime, '{y}-{m}-{d}') }}</span>
    </template>
  </el-table-column>
  
  <el-table-column label="操作" align="center" width="180">
    <template #default="scope">
      <el-button link type="primary" icon="Edit" @click="handleUpdate(scope.row)" v-hasPermi="['system:user:edit']">编辑</el-button>
      <el-button link type="primary" icon="Delete" @click="handleDelete(scope.row)" v-hasPermi="['system:user:remove']">删除</el-button>
    </template>
  </el-table-column>
</el-table>
```

**要点**:
- `v-loading` 显示加载状态
- `@selection-change` 处理多选
- `:show-overflow-tooltip` 长文本省略提示
- `dict-tag` 显示字典标签
- `parseTime` 格式化时间

### 4. 分页组件

```vue
<pagination
  v-show="total>0"
  :total="total"
  v-model:page="queryParams.pageNum"
  v-model:limit="queryParams.pageSize"
  @pagination="getList"
/>
```

**要点**:
- `v-show="total>0"` 有数据时显示
- `v-model:page` 和 `v-model:limit` 双向绑定
- `@pagination` 触发数据加载

### 5. 新增/编辑对话框

```vue
<el-dialog :title="title" v-model="open" width="600px" append-to-body>
  <el-form ref="userRef" :model="form" :rules="rules" label-width="80px">
    <el-form-item label="用户名" prop="userName">
      <el-input v-model="form.userName" placeholder="请输入用户名" />
    </el-form-item>
  </el-form>
  
  <template #footer>
    <div class="dialog-footer">
      <el-button type="primary" @click="submitForm">确 定</el-button>
      <el-button @click="cancel">取 消</el-button>
    </div>
  </template>
</el-dialog>
```

**要点**:
- `:title="title"` 动态标题
- `ref="userRef"` 用于表单验证
- `:rules="rules"` 验证规则
- `append-to-body` 防止层级问题

---

## 核心方法模式

### 查询列表

```javascript
const getList = () => {
  loading.value = true
  listUser(queryParams.value).then(response => {
    userList.value = response.rows
    total.value = response.total
    loading.value = false
  })
}
```

### 搜索和重置

```javascript
const handleQuery = () => {
  queryParams.value.pageNum = 1
  getList()
}

const resetQuery = () => {
  proxy.resetForm("queryRef")
  handleQuery()
}
```

### 新增

```javascript
const handleAdd = () => {
  reset()
  open.value = true
  title.value = "添加用户"
}
```

### 编辑

```javascript
const handleUpdate = (row) => {
  reset()
  const userId = row.userId || ids.value
  getUser(userId).then(response => {
    form.value = response.data
    open.value = true
    title.value = "修改用户"
  })
}
```

### 表单提交

```javascript
const submitForm = () => {
  proxy.$refs["userRef"].validate(valid => {
    if (valid) {
      if (form.value.userId != undefined) {
        updateUser(form.value).then(response => {
          proxy.$modal.msgSuccess("修改成功")
          open.value = false
          getList()
        })
      } else {
        addUser(form.value).then(response => {
          proxy.$modal.msgSuccess("新增成功")
          open.value = false
          getList()
        })
      }
    }
  })
}
```

### 删除

```javascript
const handleDelete = (row) => {
  const userIds = row.userId || ids.value
  proxy.$modal.confirm('是否确认删除？').then(function() {
    return delUser(userIds)
  }).then(() => {
    getList()
    proxy.$modal.msgSuccess("删除成功")
  }).catch(() => {})
}
```

### 多选处理

```javascript
const handleSelectionChange = (selection) => {
  ids.value = selection.map(item => item.userId)
}
```

### 表单重置

```javascript
const reset = () => {
  form.value = {
    userId: undefined,
    userName: undefined,
    nickName: undefined
  }
  proxy.resetForm("userRef")
}
```

---

## 详情页模式

**源码**: [`monitor/job/detail.vue`](../../ruoyi-ui/src/views/monitor/job/detail.vue)

```vue
<template>
  <div class="app-container">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>任务详情</span>
          <el-button @click="goBack">返回</el-button>
        </div>
      </template>
      
      <el-descriptions :column="2" border>
        <el-descriptions-item label="任务 ID">{{ job.jobId }}</el-descriptions-item>
        <el-descriptions-item label="任务名称">{{ job.jobName }}</el-descriptions-item>
        <el-descriptions-item label="任务组名">{{ job.jobGroup }}</el-descriptions-item>
        <el-descriptions-item label="执行方法">{{ job.method }}</el-descriptions-item>
        <el-descriptions-item label="状态">
          <dict-tag :options="sys_normal_disable" :value="job.status" />
        </el-descriptions-item>
      </el-descriptions>
    </el-card>
  </div>
</template>

<script setup name="JobDetail">
import { getJob } from "@/api/monitor/job"

const route = useRoute()
const router = useRouter()
const job = ref({})

// 获取详情
const loadDetail = () => {
  getJob(route.params.id).then(response => {
    job.value = response.data
  })
}

// 返回
const goBack = () => {
  router.back()
}

onMounted(() => {
  loadDetail()
})
</script>

<style scoped>
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
</style>
```

---

## 表单页模式

### 基础表单

```vue
<template>
  <div class="app-container">
    <el-form ref="formRef" :model="form" :rules="rules" label-width="100px">
      <el-card title="基本信息">
        <el-row>
          <el-col :span="12">
            <el-form-item label="用户名" prop="userName">
              <el-input v-model="form.userName" placeholder="请输入用户名" />
            </el-form-item>
          </el-col>
          
          <el-col :span="12">
            <el-form-item label="邮箱" prop="email">
              <el-input v-model="form.email" placeholder="请输入邮箱" />
            </el-form-item>
          </el-col>
          
          <el-col :span="24">
            <el-form-item label="备注" prop="remark">
              <el-input v-model="form.remark" type="textarea" :rows="4" placeholder="请输入备注" />
            </el-form-item>
          </el-col>
        </el-row>
      </el-card>
      
      <div class="form-actions">
        <el-button type="primary" @click="submitForm">提交</el-button>
        <el-button @click="cancel">取消</el-button>
      </div>
    </el-form>
  </div>
</template>

<script setup>
const formRef = ref(null)
const form = ref({
  userName: undefined,
  email: undefined,
  remark: undefined
})

const rules = ref({
  userName: [{ required: true, message: "用户名不能为空", trigger: "blur" }],
  email: [
    { required: true, message: "邮箱不能为空", trigger: "blur" },
    { type: "email", message: "邮箱格式不正确", trigger: "blur" }
  ],
  remark: [{ required: true, message: "备注不能为空", trigger: "blur" }]
})

const submitForm = () => {
  formRef.value.validate(valid => {
    if (valid) {
      // 提交逻辑
    }
  })
}

const cancel = () => {
  // 取消逻辑
}
</script>

<style scoped>
.form-actions {
  margin-top: 20px;
  text-align: center;
}
</style>
```

---

## 常用页面类型

### 1. 登录页

**源码**: [`views/login.vue`](../../ruoyi-ui/src/views/login.vue)

**特点**:
- 用户名/密码输入
- 验证码显示和刷新
- 记住登录（可选）
- RSA 密码加密

### 2. 首页

**源码**: [`views/index.vue`](../../ruoyi-ui/src/views/index.vue)

**特点**:
- 工作台/仪表盘
- 数据概览
- 快捷入口

### 3. 个人中心

**源码**: [`views/system/user/profile/index.vue`](../../ruoyi-ui/src/views/system/user/profile/index.vue)

**子页面**:
- `userInfo.vue` - 基本信息
- `resetPwd.vue` - 修改密码
- `userAvatar.vue` - 修改头像

### 4. 错误页面

**源码**: `views/error/`

- `404.vue` - 页面未找到
- `401.vue` - 未授权

---

## 最佳实践

### 1. 使用 Composition API

```vue
<script setup name="UserList">
// 组件名称，用于 keep-alive
const { proxy } = getCurrentInstance()
const route = useRoute()
const router = useRouter()

// 响应式数据
const loading = ref(true)
const userList = ref([])

// 方法定义
const getList = () => { ... }

// 生命周期
onMounted(() => {
  getList()
})
</script>
```

### 2. 统一错误处理

```javascript
const handleSubmit = async () => {
  try {
    await addUser(form.value)
    proxy.$modal.msgSuccess('新增成功')
    getList()
  } catch (error) {
    // 错误已在拦截器处理
  }
}
```

### 3. 表单验证优化

```javascript
// 定义验证规则
const formSchema = {
  userName: [
    { required: true, message: "用户名不能为空", trigger: "blur" },
    { min: 2, max: 20, message: "用户名长度必须在 2 到 20 个字符之间", trigger: "blur" }
  ],
  email: [
    { required: true, message: "邮箱不能为空", trigger: "blur" },
    { type: "email", message: "邮箱格式不正确", trigger: "blur" }
  ]
}

const rules = ref(formSchema)
```

### 4. 字典数据使用

```javascript
// 使用 useDict 加载字典
const { sys_normal_disable, sys_user_sex } = useDict('sys_normal_disable', 'sys_user_sex')

// 在模板中使用
<dict-tag :options="sys_normal_disable" :value="scope.row.status" />
```

### 5. 权限控制

```vue
<!-- 按钮权限 -->
<el-button v-hasPermi="['system:user:add']">新增</el-button>

<!-- 角色权限 -->
<el-button v-hasRole="['admin']">管理员操作</el-button>

<!-- 函数判断 -->
<el-button v-if="checkPermi(['system:user:add'])">新增</el-button>
```

### 6. 列显隐配置

```javascript
const columns = ref([
  { key: 0, label: `用户 ID`, prop: `userId`, visible: true },
  { key: 1, label: `用户名`, prop: `userName`, visible: true },
  { key: 2, label: `状态`, prop: `status`, visible: true }
])
```

```vue
<el-table-column v-for="col in columns" :key="col.key" :prop="col.prop" :label="col.label" :visible="col.visible" />
```

---

## 页面命名规范

```vue
<script setup name="UserList">
<!-- 组件名称必须填写，用于 keep-alive 缓存 -->
</script>
```

**命名规则**:
- 列表页：`XxxList`
- 详情页：`XxxDetail`
- 编辑页：`XxxEdit`
- 新增页：`XxxAdd`

---

## 常用样式类

```scss
.app-container {
  padding: 20px;
}

.mb8 {
  margin-bottom: 8px;
}

.mr5 {
  margin-right: 5px;
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.form-actions {
  margin-top: 20px;
  text-align: center;
}

.small-padding {
  padding: 5px;
}

.fixed-width {
  width: 180px;
}

.show-path {
  :deep(.el-dialog) {
    width: 800px;
  }
}
```

---

**文档版本**: 1.0  
**最后更新**: 2026-04-07  
**基于版本**: RuoYI v3.9.2
